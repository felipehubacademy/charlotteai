# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native — gerado por expo prebuild, nao commitar
/android/
/ios/
# Excecao: codigo nativo dos nossos modulos locais (modules/*) deve ser commitado
!/modules/*/android/
!/modules/*/ios/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# Node
node_modules/
npm-debug.*
yarn-debug.*
yarn-error.*

# Env files
.env
.env.local
.env.production

# EAS + Firebase credentials (never commit)
google-service-account.json
google-services.json

# OS
.DS_Store
*.pem

# TypeScript
*.tsbuildinfo
